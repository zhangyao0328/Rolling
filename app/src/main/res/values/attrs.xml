<?xml version="1.0" encoding="utf-8"?>
<resources>


    <declare-styleable name="layout_search">
        <attr name="ed_hint" format="string"></attr>
    </declare-styleable>

    <declare-styleable name="layout_toolbar">
        <attr name="arrow_left" format="reference"></attr>
        <attr name="title" format="string"></attr>
        <attr name="arrow_right" format="string"></attr>
    </declare-styleable>

    <!-- 缩放的 ImageView -->
    <declare-styleable name="ScaleImageView">
        <!-- 缩放比例 -->
        <attr name="scaleRatio" format="float" />
    </declare-styleable>

    <!-- 按照比例显示的 FrameLayout -->
    <declare-styleable name="RatioFrameLayout">
        <!-- 宽高比例 -->
        <attr name="sizeRatio" format="float" />
    </declare-styleable>

</resources>